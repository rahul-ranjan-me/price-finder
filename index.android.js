import React, { Component } from 'react';
import {
    AppRegistry,
} from 'react-native';

import priceFinder from './components/App';

AppRegistry.registerComponent('priceFinder', () => priceFinder);